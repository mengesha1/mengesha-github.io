export function isVowel(letter:string):boolean{
    let char:string;
    if (letter === 'a' || letter === 'e' || letter === 'i' || letter === 'o' || letter === 'u'){

    return true;
    }else{
        return false;
    }
    

}